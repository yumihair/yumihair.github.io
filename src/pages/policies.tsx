import Title from '../components/Title';

function Policies() {
  return (
    <div style={{ minHeight: '110vh', position: 'relative' }}>
      <Title name="Policies" cursiveName="faqs" />
      </div>
    );
}

export default Policies;
